using cat -n usernames.txt | grep cult

we can find the number of the row

thus we cat -n passwords.txt | grep 378

then we get the password but it looked like some ceasar cipher

using this site we could cycle through each https://cryptii.com/pipes/caesar-cipher
and found it was 13 
